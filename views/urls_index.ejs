<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include partials/_header %>
</head>
<body>
  <% for (let link in urls) { %>
    <li><%= link %> <%= urls[link] %> </li>
  <% } %>
</body>
</html>